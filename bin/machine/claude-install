#!/bin/bash
# Host-side wrapper around the canonical script. Available on the
# mpd-machine VM after `mpd --setup` adds `bin/machine` to PATH.
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec bash "$SCRIPT_DIR/../../assets/runtime-base/tools/claude-install" "$@"