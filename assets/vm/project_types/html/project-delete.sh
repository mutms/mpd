#!/bin/bash
# project-delete.sh <project-name>
# Run by `mpd delete <project>`. A static site owns nothing outside its
# own directory - no database, no service, no build output - so there is
# nothing to clean up here.
set -euo pipefail

exit 0
