#!/bin/bash
# Tool: run Moodle cron for the current project (one cycle).
# Cwd-walks via mpd-env.sh to find the project context.
set -euo pipefail
source /opt/mpd/assets/runtime/project_types/moodle/scripts/mpd-env.sh

moodle_run admin/cli/cron.php "$@"
